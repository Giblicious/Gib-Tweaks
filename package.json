{
  "name": "gib-tweaks",
  "version": "0.4.3",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test scripts/status-bar.test.mjs",
    "check": "npm run build && npm test && node scripts/check.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
